import { Box, Typography, Grid } from "@mui/material";

const NotFound = () => {

    return <Box mt={22}>
        <Grid container>
            <Grid item xs={4}>
            </Grid>
            <Grid item xs={4}>
                <h1> Page not found.. ! </h1>
            </Grid>
        </Grid>
    </Box>
}

export default NotFound;